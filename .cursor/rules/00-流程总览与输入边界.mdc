---
description: 匠石开发须遵循 00 流程总览：七阶段基线、输入信封与稳定原则
alwaysApply: true
---

# 00 流程总览与输入边界

权威文本：`doc/design/00-流程总览与输入边界.md`。改流程、输入或模块边界时先读该文，细节以对应模块文档为准，不在错误模块展开。

## 稳定原则（不因基线实验而改）

1. 外部文字输入必须有对象引用。
2. 无对象、暂定对象、已确认对象不可混同。
3. 原始输入、派生理解、长期个人内容分开记录。
4. 新主体面未完成现实，不得在认知前凭空生成。
5. 正常活动不无声改写长期个人世界。
6. 重要调整须写入 `doc/决策记录.md`，并说明对已有模块和数据的影响。

## 当前实现范围

- 只处理 `external + text + complete`。多模态、流式、渠道层对象映射、身份确认回合、内部系统事件：接口可预留，本阶段不实现。
- 外部主链路七阶段顺序：对象解析与落位（含 16 追加经历）→ 活跃区只读 `ContextViewState`（不调模型、不重切、不调整）→ 状态组装（只读已有记录，不编造，不装载事件）→ 活动建立（不挂载事件）→ 认知（产出 `response_plan`，可追加召回；主链路不落 thought）→ 06 标记 → 行动 → 16 按评估更新视图 → 07 收尾 → 30 投递。顺序变更须经决策记录。
- 内部输入（反思等）不走对象解析与活跃区外部语义链路；来源标为 `system`。
- 正常活动可记录候选认知，不在主链路自动升格为长期价值、关系、能力或自我理解。

## 输入门禁

```text
# 错误：无对象仍落位并创建活动
envelope.object is None → persist(external_input); create_activity()

# 正确：完全缺失对象则契约拒绝
envelope.object is None → reject; 不落位、不创建活动
# 有引用但未确认 → 暂定对象候选，按 01 置信度处理；低于阈值则阻断，不进入后续活动
```

## 模块边界

- 05 只产出 `response_plan` 与上下文候选；记忆召回、落位、指标、活动标记和视图编辑不在 05 实现。
- 06 只校验并标记 `response_plan`；原认识状态机暂缓，不经 06 自动升格长期认识。
- 07 只关闭活动；记忆投递归 30。新事件须带来源，不在认知前凭空生成。
- 输入信封、外部/内部入口只在 00 定义；各模块引用，不重复定义。
- 新模块须在 00 的「记录与评价总览」登记或引用。
