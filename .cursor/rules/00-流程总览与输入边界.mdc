---
description: 匠石开发须遵循 00 流程总览：七阶段基线、输入信封与稳定原则
alwaysApply: true
---

# 00 流程总览与输入边界

权威文本：`doc/design/00-流程总览与输入边界.md`。改流程、输入或模块边界时先读该文，细节以对应模块文档为准，不在错误模块展开。

## 稳定原则（不因基线实验而改）

1. 外部文字输入必须有对象引用。
2. 无对象、暂定对象、已确认对象不可混同。
3. 原始输入、派生理解、长期个人内容分开记录。
4. 不设主体面未完成现实或 `concern`；组装只读已有记录，不在认知前编造长期跟进义务。
5. 正常活动不无声改写长期个人世界。
6. 重要调整须写入 `doc/决策记录.md`，并说明对已有模块和数据的影响。

## 当前实现范围

- 只处理 `external + text + complete`。多模态、流式、渠道层对象映射、身份确认回合、内部系统事件：接口可预留，本阶段不实现。
- 外部主链路七阶段顺序：对象解析与落位（含 16 追加经历）→ 02 只读上一份活跃区（不调模型、不重切；不含本轮尚未编排的输入）→ 状态组装（上一活跃区 + 本轮输入 + 对象 + 身份 + 08/09；只读已有记录，不编造）→ 活动建立 → 认知（产出 `response_plan` 与整份新活跃区正文；对象确认可空；本轮不打分；不同轮补召回；主链路不落 thought）→ 06 标记 → 行动 → 16 保存整份现场正文（空则保留上一份）→ 07 收尾 → 30 投递（批次须分型）。有效性分析在旁路。顺序变更须经决策记录。初次使用或账本/活跃区复位后仍走同一条路径；空活跃区合法。
- 内部输入（反思等）不走对象解析与活跃区外部语义链路；来源标为 `system`。反思为占位，不是记忆整理。
- 正常活动可记录候选理解，不在主链路自动升格为长期价值、关系、能力或自我理解。
- 事件只在 09 内部；主链路与 02 / 03 / 05 / 07 暂不使用。

## 输入门禁

```text
# 错误：无对象仍落位并创建活动
envelope.object is None → persist(external_input); create_activity()

# 正确：完全缺失对象则契约拒绝
envelope.object is None → reject; 不落位、不创建活动
# 有引用但未确认 → 暂定对象候选，按 01 置信度处理；低于阈值则阻断，不进入后续活动
```

## 模块边界

- 05 产出 `response_plan` 与整份新活跃区正文。不检索记忆、不投递记忆、不写 16、不标记活动、不同轮补召回。有效性分析不插入 05/06/10/07。
- 06 只校验并标记 `response_plan`。
- 07 只关闭活动；记忆投递归 30。
- 10 分发语言记录、独立语音占位与肢体；语音不走机器人接口。
- 输入信封、外部/内部入口只在 00 定义；各模块引用，不重复定义。
- 新模块须在 00 的「记录与评价总览」登记或引用。
