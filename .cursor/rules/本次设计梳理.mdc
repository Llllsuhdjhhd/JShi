---
description: tune/05-flow：优化 05 流程；按模块先文档后代码；先读记事本「下一份」
alwaysApply: true
---

# 本次设计梳理（本分支）

当前分支：`tune/05-flow`（从本地 `main` @ `2136ae1` 分出）。权威记事：`doc/design/本次梳理.md`。

每次动手前先读记事本的「下一份」；改完立刻勾进度、写变更日志。不要凭会话记忆代替记事本。

## 本分支动作

- **按模块**：该模块文档校准并经用户确认 → 只改该模块代码与对应测试 → 再进入下一模块文档。不要先改完全部 `doc/design/` 再写代码。
- 00 无独立代码包，只改文档。跨模块缺口记在被依赖模块（例如对象摘要进工作集归 03，不在 01 顺手改组装）。
- 缺号（11/14）不编造文档。12 已迁 300，勿在 12 写规范。
- 用户说「写方案」才写 `方案(coding)`；说「写代码 / 按这个改代码」才动代码。

## 本轮不可忘的口径

- 本轮上下文 = 03 组装全体（输入、对象、身份、个人世界、回忆、上一活跃区、200 可见人话经 `tool_input` 原样装载可空）。活跃区是 05 按选中风格整份重写的工作上下文，16 保存。00 / 02 / 16 / 概念词汇已按 D-009 回写。不要【在途工具】标题，不要叫醒。
- 本轮原话走 `input_text` 进模型；不在认知开始前并入上一活跃区。禁止再写「本轮输入不进入这轮上下文」。
- 05 必出 `response_plan` 与整份新活跃区正文。本轮不做记忆打分。对象确认可空。不同轮补召回（D-008）。记忆批次须分型后再交 09。风格包配置名不进提示词。要用工具只交指示与一句 need，不填模板/参数（I-017）。
- 对外契约是 `response_plan`；主链路不落 thought；`wait` 结束本轮活动。06 只校验并标记 `response_plan`；不消费打分/缩减/聚焦，不写经历、不编活跃区、不投递记忆。
- 有效性分析是旁路（原独立评价系统已更名）；失败不挡说话。I-014 已采纳。
- 07 一律关闭为 `completed`（含 `wait`）；不写经历、不改活跃区、不投递记忆、不处理事件。编排层凭 handoff 调 30。
- 08 是 100+ 子模块的薄壳：合并有序列表、按 id 去重、按等级取量。不排序、不按输入筛选。`concern` 已废（D-007），不装、不以新名字复活。08 每轮仍装真源，不因风格化现场而跳过。
- 10 分发 `response_plan`：有 verbal 则落记录并走独立语音占位；embodied 走肢体占位。二者不混。
- 事件只在 09 内部；02 / 03 / 05 / 07 暂不使用。00 不写「认知后创建事件」。
- 自省（300）占位雏形，与记忆分开写；不并入 200。
- 已取消的认识状态机：00 / 01 不再提。
- 渠道已绑定的对象置信度最高；声纹等载体相应提高；正文名字匹配不是最高。
- 名称与别名字段都必须带；不重名按正常处理；重名必须用记忆等消歧，不得任取。
- 归属判断已废弃；I-001–I-005 不纳入本轮契约。I-006 已写入 02：初次/复位与后续同一流程，空视图合法。

```text
# 错误
01 文档刚改完就去改 02 文档，把 01 代码留到全部文档结束。

# 正确
记事本下一份是「02 文档」→ 只改 02 口径 → 回写记事本。
```
