---
description: 按协作流程推进：先讨论，再方案，后代码；未经用户确认不得自行改文档或代码
alwaysApply: true
---

# 协作流程

权威文本：`doc/协作流程.md`。顺序不可跳：

```text
讨论（设计文档）→ 用户确认 → 方案(coding) → 用户确认 → 代码与测试 → 用户说「提交」才提交
```

未轮到的步骤不要做。不要自己决定去改文件。

## 闸门

- 设计未讨论、用户未确认：只陈述判断、分歧和牵连，**不改** `doc/design/`、方案、代码。
- 方案未写清范围、落点、测试与回退，或用户未确认方案：**不动代码**。
- 文档落后于代码时，先改文档并等确认，再改代码。
- 用户说「提交」才 git commit；未指示不推送。

用户明确说「按这个改 / 写方案 / 写代码 / 提交」时，只做该一步，不顺手做下一步。

```text
# 错误
用户问 wait 是什么意思。
助手直接改 process.py，并改 06/10 文档。

# 正确
用户问 wait 是什么意思。
助手说明：wait ≠ think；本轮活动仍结束，下一轮新建活动。
给出方案要点，等用户确认后再写 方案(coding)，再动代码。
```
