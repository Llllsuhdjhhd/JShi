# 工具使用（设计文档）

> 模块文档。术语以《概念词汇》为准，流程位置见《00 流程总览与输入边界》。
>
> 状态：初稿已按 I-017 / I-018 改口径。编号 **200** 为工具模块总调度；内部为 205 策划 skill、206 引擎适配、210 记挂账本、包装 skill。已进 00 模块地图。
>
> **实现状态与设计口径分开写。** 主流程通道已落地。内部按 `方案(coding)/200.md`：210 只作账本；两 skill 写定义与可见人话；206 边读边入账。不改 Pi 内核。报价只由 200/205 写一条；引擎不 yield `estimate`。主流程默认 Stub，`JSHI_TOOL_ENGINE=pi` 才接 Pi。

边界原则：

- **D-004**：能力实现只出候选与过程反馈，无长期写入权。200 / Pi / 记挂都不写 08、不改认识状态、不对人开口。
- **D-007**：记挂不是 concern。不进 08、不挂起活动、不每轮全量装入工作集。03 只装 200 交出的、该对象、可见、有上限的人话（`tool_input`），不改写。

## 1. 定位

工具使用让匠石在 05 或自省判定「要用工具」之后，把这件事交给 **200** 落到现实，并把过程跟随收进记挂；值得看见的跟随经 03 回到心智。

对外通信只经 200。05、03、自省不直连 Pi，不直连记挂账本。

| 角色 | 做什么 | 不做什么 |
|------|--------|----------|
| **05** | 判定要不要用；一句 `need`；对人说话（口头须带上工具需求） | 不填模板/参数、不等工具、不跑工具 |
| **200** | 总调度：收下交接、策划定义、发出请求、把跟随交给记挂、对 03 交出可见人话 | 不对人开口、不写活跃区 |
| **205** | `ToolPlanSkill`：选模板、填参数、写估价 | 不跑 Pi、不对人开口 |
| **206** | 对引擎（现为 Pi）：发会话、把事件译成信封并写入对应 210 | 不改 Pi 内核、不思考 |
| **记挂（210）** | 一次请求发出后的账本：收阶段性事实，通知 200 | 不选模板、不包装、不对人开口 |
| **包装** | `ToolWrapSkill`：读这一本 210，产出 `{visible, summary}` | 不跑 Pi、不读其它记挂 |
| **Pi** | 工具**内容**（skill / command 的实际执行） | 不发明匠石的定义槽位、不直连 05 |
| **10** | 只分发已校验的口头和肢体 | 不执行工具 |

调试：CLI `jshi tool` 仍是第三条路径，不经主体过程。

## 2. 实现状态（以代码为准）

代码在 `src/jshi/tool/`。主链路：写场后 `ToolService.intake`（不等策划/执行），07 照关。process / 03 不直连 `HangStore` / `ToolRunner`。

| 部分 | 落点 | 现状 |
|------|------|------|
| 请求 / 反馈信封 | `contract.py` | 已实现。见 §14。`command` 是引擎命令名，与匠石 `template` 分开。 |
| 引擎端口 | `ToolEngine` / `ToolModule.iter_feedback` | Runner 逐条入账。`submit` 仍可整包给旁路。引擎不 yield `estimate`。`cancel` 停该次会话。 |
| 占位引擎 | `StubEngine` | 测试与默认主流程。反馈 `progress` → `result`。 |
| 引擎切换 | `JSHI_TOOL_ENGINE` | `stub`（默认）或 `pi`。live 未稳前不改默认。 |
| Pi 适配 | `PiEngine` | live；边读边交出；未映射事件丢掉；`stderr=DEVNULL`。墙钟超时 → `result/failed`「工具执行超时」。`cancel` 杀子进程。`list_commands` 读 `get_commands`（name + description）。prompt 用 `command`，并带 `expected_result` / `field_ref`。不改 Pi 内核。 |
| 交接账本 | `intake.py` / `tool.jsonl` | 追加快照，加载按 id 取最后一行。状态：`received` → `launched` \| `failed` \| `cancelled`。无 `planned`。`meta.model_tag` 记策划模型。 |
| 200 门面 | `service.py` | `intake` / `list_visible` / `cancel`。一本一包装队列。launch 时写**唯一**一条 `estimate`。`cancel` 先停 runner 再注销账本。 |
| 策划 | `skill/tool_plan.py` | 同时读匠石 catalog 与引擎 `list_commands`。有引擎目录则必须选其中已有的 `command`。目录在第一次策划时再取（不在 `talk` 启动时同步拉）；读不到或超时则空，仍可读匠石槽位。目录查询单独短超时（默认 5 秒），与执行超时（默认 120 秒）分开。 |
| 包装 | `skill/tool_wrap.py` | 写回 `visible`/`summary`。一律经 `set_wrap` 截到 80 字。无 skill 时 RESULT 规则占位。进度包装不把 status 打成 `notified`。 |
| 记挂存储 | `hang.py` | 发出请求后才建。追加快照。`visible`/`summary`；读旧键 `notify_caller`。`notified` 仅终态且可见。`list_open` 留给测试与巡检，主流程用 `list_visible`。 |
| 模板说明 | `templates/catalog.json` | 匠石槽位（含 `time_note` / `exception_note`）。不拿它冒充 Pi 命令目录，不手扩来凑工具名。 |
| 05 只出指示 | `ToolUseIntent` | 已按本文：`use_tool` + `need`。 |
| 03 从 200 装可见条目 | `ToolSource` | 已实现。`list_visible` 的 `summary` 原样进 `tool_input`，不进工作集分片。**每轮仍装**仍为可见的那句，不是装过一轮就消失。 |
| 边读边入账 | `port.py` / `pi_engine.py` | 已实现。 |

## 3. 术语（不要混）

**工具**  
把一件**只有离开主体边界才办得到**的事，交给主体之外的执行体去做，并把结果带回主体的能力。
三条**同时成立**才算工具：① 在主体边界之外（要的信息或要造成的影响不在匠石自己的记录、心智与
个人世界里）；② 主体自己办不到 / 不该自己办（凡"想一想、查自己已有的记录"能解决的都不是）；
③ 有代价、有不确定、可能不可逆（时间 / 费用 / 额度，可能失败或被拒，改动了外面可能收不回来）。
再加一条归属判据：**经 200 才算工具**。一句话口径：**「离开主体才办得到吗？有代价吗？经 200 吗？」**

| 事 | 算工具吗 | 为什么 |
|----|----------|--------|
| 搜网页、查天气、发消息、下单、控制设备 | **是** | 三条全中，且经 200 |
| 叫大模型生成 / 做认知 | 不是 | 走 `ModelPort`，是「能力实现」，不经 200 |
| 召回记忆、读账本、装载个人世界 | 不是 | 主体自己的记录，无外部性、无代价 |
| 开口说话、做肢体动作 | 不是 | 那是 10 的行动（`response_plan` 的 verbal / embodied） |
| 问第三人（I-011 追加召回） | 不是 | 认知侧召回，不是外部执行 |
| 调记忆引擎 REMS | 不是 | 记忆是匠石自己的一部分（可替换实现，不是外部工具） |

**交接（intake）**  
05 或自省交给 200 的那一份背景，供策划用。还不是记挂。含：对象、活动、现场引用、`need`、本轮口头（外部路径应有）。

**定义模板**  
匠石侧预置。通用基础模板规定槽位（意图、预估时间/费用、可能结果、风险、参数等）；专用模板在此之上。200/205 按模板填好，才叫一份**定义**。

**工具内容**  
Pi 内部的 skill / command / 扩展。负责真正去搜、跑、调。目录可用 `get_commands`，与定义模板不是同一套「模板」。

**工具请求**  
200 按定义向引擎发出的那一次调用（代码里的 `ToolRequest`）。

**记挂（210）**  
请求**已经发出之后**的账本：跟着这一次请求收阶段性事实（不要思维链），有新事实就通知 200。不做策划、不包装。无请求则无记挂。多本可并行。

**引擎**  
可替换执行端口。真实现是 Pi；Stub 给测试与默认主流程。`JSHI_TOOL_ENGINE=stub|pi` 切换。设计不写成「只能是 Pi」。

## 4. 核心口径

1. 05 只说「要用」和一句 `need`。外部路径上，口头必须有，且口头本身带上工具需求。
2. 200 读的是交接，不再向 05 要定义。现场引用只作处理依据，不拷进当前活跃区。
3. 先交接、再策划、**发出请求之后才建记挂并跟随**。
4. 策划失败：还没有请求，也就没有记挂。200 经同一对外口把失败摘要交给 03/05，不假装成工具跟随。
5. 调用一开始就有且仅有一条 `estimate`，由 200 按 205 的槽位写入记挂。引擎不报价、不 yield `estimate`，只报真实进度与结果。数字与风险来自定义模板 + 该工具自己的说明（Pi 的 `description` / `SKILL.md`），不改 Pi 内核去加字段。
6. 同步只约束 **200 内部**（一条 Pi 会话按事件顺序读、反馈马上入账）。主流程、自省可以多次、交错 `intake`。05 这一拍仍不等 200 跑完。
7. 不改 Pi 内核。过程进度用已有 `tool_execution_*` 事件；206 边读边交给记挂。
8. 对 05 的材料经 03 的 **`tool_input`** 装载，不是【在途工具】长节，不进片场。200 决定何时交出、交出一句合适的人话；匠石决定是否告诉对方。不叫醒、不插内部活动。

## 5. 职责与对外口

```text
05 / 自省  --intake-->  200
03          --list_visible-->  200
05          --cancel-->  200

200 内部：
  交接保存（调度上下文，不是记挂）
  205 ToolPlanSkill 填定义
  发出请求 → 建立 210 → 206↔引擎（边写入该本）
  210 通知 200 → ToolWrapSkill 写回 visible/summary
```

200 交给 03 的是一句人话（`visible` 且非空的 `summary`）。仍不是 10 的口头：200 不对人开口，开口仍是 05 的 `response_plan`。

内部编号（对外仍是 200）：

- **205** 策划 skill  
- **206** 引擎适配  
- **210** 记挂账本  
- **包装 skill** 不另占对外号  

空号不必占。

## 6. 流程

```text
外部这一拍
  … → 05 回复调用：response_plan + 可选 use_tool/need
  → 06 → 10（口头须带工具需求）→ 写场
  → 若要用：200.intake(交接)     ← 不等待执行
  → 07 completed

200（可与后续活动并行；内部同步读流）
  保存交接
  205 ToolPlanSkill（读交接 + 匠石槽位 + 引擎 list_commands）
  定义无效 → 可见失败摘要，经 03 回 05；无 210
  定义有效 → 发出请求、建 210、由 200 立刻写一条 estimate
  206 读引擎流，事件即时写入**这一本** 210（丢掉思维链）
  有阶段性事实 → 该本的包装队列 → ToolWrapSkill → 写回 visible/summary
  包装不挡 206 读流；多本各排各的队

03 下一轮（外部正常组装，不叫醒）
  list_visible(当前对象) → tool_input（原样；无则整路不出现）
  05 决定是否告诉对方他先前问的结果
```

一次指示可拆多条定义、多条请求、多条记挂。各条跟随并行。

自省入口（300）同样 `intake`，可以没有对人的口头。自省是 300，不并入 200。

## 7. 05 这一拍

提示词增加：本轮要不要用工具。规则从简：需要外部世界、当前上下文不够、能用一句话说清时才标要用。

输出（可空，不是 10 的通道）：

```text
use_tool    要用则为真；省略或假 = 不用
need        一句话意图（为什么用、要什么）；不是模板名，不是参数
```

不要：`template`、`params`、`expected_result`、`budget`、`ask`。那些由 205 填。

外部路径：`use_tool` 为真时，`response_plan` 须有口头，且口头说出工具需求。禁止只在内部打标、对人一声不吭。内部自省不受这条约束。

05 本轮不等 200。活动不因工具变成 `waiting`。工具结果不进本轮 verbal，不改写场正文。10 不调用工具。

已装载的工具反馈（03 的 `tool_input`，来自 200）应让 05 看见。具体怎么处理由匠石决定，合适的时候告诉对方。不要当成片场里已经说过，也不要当成对方刚说的话。不要无故再开同样一条。写场调用不带这一路。

## 8. 交接（不是记挂）

`intake` 时 200 记下：

```text
subject_id / object_id / activity_id / origin
need
verbal                 # 外部路径：本轮已经对人说的、含工具需求的那句
field_ref              # {activity_id, zone_kind: wood|persona, zone_rev}
                       # 木头：写场后的 ContextViewState.version（无则 activity_id）
                       # 人格：无片场版本则 activity_id
```

205 再问模型时只读这份交接（外加定义模板与工具说明）。不向 05 再要一份定义。禁止把 `rewritten_context` 全文写入交接或记挂。

现场引用粒度本轮够用即可；不新造 16 的 revision 字段。

## 9. 定义模板与一开始的反馈

通用基础模板至少含：意图、预估时间、预估费用、可能结果、风险/副作用、参数槽、期望形态。专用模板继承这些槽，侧重点不同。

**调用一开始**（请求发出、内容尚未跑或刚开跑）就必须有一条 `estimate`，由 205 按模板槽位写出，由 200 立刻写入该条记挂。引擎不得再写一条。

估价来源：

1. 定义模板的槽位（形状归匠石）；  
2. Pi 侧该工具自己的说明：`get_commands` 的 `description`，必要时读 `path` 指向的 `SKILL.md`。Pi **没有**结构化的时间/费用字段，约定写在说明文本里。

不改 Pi 内核去新增协议字段。自带 bash 等只有短描述时，205 用通用模板填默认（时长未知、有执行风险），不要去改 Pi。

`ask=propose_only` 仍保留在请求信封上，主路径默认 `execute`。先报价再决定是否跑内容，由 200 内部选用，不回到 05 填。

## 10. 引擎与 Pi（不改内核）

206 只翻译：发 `prompt`、按 LF JSONL 读事件、译成信封。

Pi 已有：`tool_execution_start` / `update`（累计 `partialResult`）/ `end`。目标是 **边读边入记挂**，不要攒到 `agent_settled` 再整包写。

进度能密到什么程度，取决于该 skill 会不会打过程输出。只有 start/end 时，跟随里就只有开始和结束。要更密，改的是那个 skill 的说明与输出习惯，不是 Pi 内核。

适配器：`timeout_s` 接到执行读循环；`list_timeout_s`（默认 5 秒）接到 `get_commands`。超时落 `result/failed`「工具执行超时」。`cancel` 杀该次子进程。可执行文件不存在或列目录失败则空目录，不让 `talk` 起不来。避免 `stderr=PIPE` 堵死。属 206，不属新模块。prompt 用引擎 `command`，不要把匠石模板名当成 Pi 命令名。`field_ref` 与 `expected_result` 写入 prompt，字段保留。

预算、权限字段在信封里预留；Pi 适配尚未用它们约束执行。失败按模板降级并给原因。

## 11. 同步

「同步」只指 200 内部：每一条已发出的请求，对应一次引擎会话，按事件顺序收，反馈马上入账。

主流程、自省会多次、交错调用 200。200 必须可重入：多条记挂并行，不得一把全局锁堵住第二次 `intake`。05 这一拍仍然不等待。

包装 skill 排在**该本 210** 自己的队列，**不挡** 206 读流，也不用一把全局锁堵住第二次 `intake`。`visible` 是包装输出，不另开 skill。

## 12. 记挂（210）

在 **200 已经发出工具请求** 时建立，跟着这一次 `request_id`。号留下，就是账本。

不做：选模板、填参数、策划、写人话、对人说话、改活跃区。

做：只增反馈流（阶段性事实，不要思维链）；有新事实通知 200；保存当前 `visible` / `summary`；按对象保管；可注销。

多条请求 = 多本 210，可同时跟随。一本交接也可拆多本 210。不同对象按 `object_id` 隔离。同一本上后一句 `summary` 覆盖给 03 看的那条；同一 `task_id` 在一轮组装里只占一条（不是两句并排），**之后每一轮只要仍 `visible` 就继续装**，不是装过一轮就从 `list_visible` 消失。进度包装可以可见，status 仍为 `open`；`notified` 只在终态且可见时打。注销走 `cancel`（停会话 + 账本）。可见句的过期/说完再藏（TTL）本轮不做。

给 03 的不是 Pi 原文，而是包装 skill 写回的 `summary`（须 `visible`）。策划失败不是记挂。程序崩溃可丢进行中的子进程；账本行仍在——可接受，巡检后补。jsonl 是追加快照：单次写入便宜，文件体积随每次包装再写一整份记录而涨。长会话、高频 progress 需要压缩或轮转时再做，本轮不自动紧缩。

策划失败不是记挂。程序崩溃可丢进行中的子进程；若请求已发出，账本行仍在（跟随中、无终态）——可接受，巡检后补。

## 13. 经 03 回到 05

可见跟随走 03 的 **另一路输入** `tool_input`，与对方的 `input_text` 并列。不是 08，不是活跃区正文，不是对方原话，不是回忆，不要【在途工具】标题。

- **200 给出**：`ToolWrapSkill` 对这一本 210 交出 `{visible, summary}`。可见 = 这句话可进 `list_visible`。
- **03**：按接口取 `summary`，原样装上。没有则这一路不出现。不改写，不叫醒，不开内部活动。等下一轮正常组装即可。仍为可见的句子**每轮都装**，不要在装载时标已交付并藏掉。
- **匠石（05）**：提示词写明——具体怎么处理由你决定，合适的时候告诉对方他先前问的结果。对人开口仍只走 `response_plan`。

写场调用不带 `tool_input`，以免写进片场当已经出口。

不要为工具反馈插内部输入 `source=system/tool`。晚一轮看见，接受。

## 14. 接口契约（对齐现有信封；调用方改为 200）

`ToolRequest` / `ToolFeedback` 仍以 `src/jshi/tool/contract.py` 为准。编排层不再由 05 JSON 填全请求；05 只交指示，200 补 `subject_id`、`activity_id`、`origin`、`ask` 等。

### 14.1 `ToolRequest`

| 字段 | 说明 |
|------|------|
| `request_id` | 关联；发出时生成 |
| `subject_id` / `activity_id` / `turn` | 溯源 |
| `origin` | `external_05` \| `introspection` |
| `need` | 来自交接 |
| `template` | 匠石侧定义模板名（槽位说明）；由 205 填 |
| `command` | 引擎侧工具内容名（Pi command / skill）；有引擎目录时必选其中已有的名字 |
| `field_ref` | 现场引用；206 写入 prompt；保留，不删 |
| `params` / `budget` / `permission` / `expected_result` / `ask` / `meta` | 205 填；主路径 `ask=execute`。`expected_result` 写入引擎 prompt。 |

### 14.2 `ToolFeedback`

信封：`event_id`、`request_id`、`kind`、`at`、`version`、`meta`；按 kind 带 `estimate` / `progress` / `result` 之一。

| kind | 主要字段 |
|------|----------|
| `estimate` | `cost_est`、`time_est_ms`、`resource_est`、`benefit`、`downside`、`need_confirm` |
| `progress` | `stage`、`step`、`partial`、用量、`note` |
| `result` | `status`、`result`、`summary`、`ideal`、`error` 等 |

`ToolStatus`：`ok` / `partial` / `failed` / `budget_exceeded` / `rejected` / `aborted`。

### 14.3 交接与记挂（设计）

交接字段见 §8。记挂在请求发出后建立，至少含：`task_id`、`request_id`、`subject_id`、`object_id`、`origin`、跟随状态、`need` / 所用模板、`field_ref`、反馈列表、`visible`、`summary`、`updated_at`。

200 对外：`intake` / `list_visible(subject_id, object_id)`（上限建议 8）/ `cancel`。

## 15. 行为规则

1. 05 / 自省只判定与开口；定义、执行、跟随都在 200 内。  
2. 引擎与记挂不写长期个人世界（D-004）。  
3. 05 发起后不等工具；本轮活动照关。  
4. 工具结果不是说话人输入；经 03 `tool_input` 原样装上，不是对方原话。  
5. 现场引用只作处理依据，不并入当前活跃区。  
6. 对人开口只由 05 做。  
7. 记挂按对象、有注销、不进 08。  
8. 10 不执行工具。  
9. 契约字段只增不改；`version` + `meta`。  
10. 不改 Pi 内核。

不纳入：05 同轮死等工具；05 填写模板/参数；把旧活跃区与结果写入当前活跃区；记挂进 08；策划尚未发出请求却先建记挂。

## 16. 记录、评价与参数

- **记录**：交接、定义、请求、跟随流、是否可见、策划失败摘要。不进 16 经历段当工具日志；不进 09（是否另建 `fact/tool_*` 未决）。  
- **评价**：可见是否过密/过疏；05 是否空头支票（口头有、intake 无）或只打标不口头。后补。  
- **参数**：`list_visible` 上限（建议 8）；包装并发上限（占位 2）。14 日后接管。  
- **I-005**：跟随与可见评估不挡 05 说话。

## 17. 验收（设计；未全部编码）

- 05 只用 `use_tool` + `need`；外部路径口头含工具需求。  
- 交接不是记挂；发出请求后才跟随。  
- 策划失败经 200→03，不是一条假跟随。  
- 请求一开始即有 `estimate`；进度边读边入账。  
- 03 只装该对象可见人话（`tool_input`，原样）；当前活跃区不被旧现场替换。写场不带这一路。  
- 10 与 08 不承担执行或保管跟随。  
- Stub 引擎反馈：`progress` → `result`（无 estimate）；走 200 `ToolService` 时反馈流恰好一条 `estimate`，且在引擎反馈之前。`propose_only` 终态 `rejected`（引擎契约仍在）。
- `cancel` 停该次引擎会话；超时有终态。`summary` 有统一上限。`list_visible` 每轮仍交出仍为可见的句子。

## 18. 与其他模块

- **依赖**：16（现场版本指向）、01（对象 id）、工具说明文本（Pi 目录，不改内核）。100 / 15 / 11 权限与介入后补。  
- **被依赖**：05（指示与看见 `tool_input`）、03（`list_visible` → `tool_input`）、00（地图）。  
- **不依赖 10**。

内部信封若将来用于定时等，对象仍从交接或记挂取、现场只读、不并入当前活跃区。工具反馈**不**用 `source=system/tool` 叫醒。

## 19. 未决

- 05「要不要用」的更细规则（现从简）。  
- 一条 need 拆多条请求（本刀策划先出 1 条）。  
- 包装过密的更细阈值。  
- 现场引用是否要段 id / 短摘录上限。  
- 是否另建 `fact/tool_call` 等事实种类。  
- Pi 适配落实预算与 `ask`。  
- 长期能力升格（V5）。  
- 可见句生命周期（过期 / 匠石开口后再藏）；本轮只靠上限 8 与 `cancel`。  
- 300 自省完整入口（`object_id` 与自省冲突须另拍）。  
- 主流程默认改 Pi（live 稳后再翻）。  
- 100 / 15 / 11 权限实装。  

已拍板、不要退回：05 不填定义；交接 ≠ 记挂；记挂只跟随已发出的请求；对外只经 200；不改 Pi 内核；同步只在 200 内部；03 不改写 200 的正文；不叫醒；不要【在途工具】长节。
