# 本次设计梳理记事

> 工作记事，不是模块契约。权威流程仍以各模块正文为准；正文改完后，在此勾进度。
> 助手规则：`.cursor/rules/本次设计梳理.mdc`（alwaysApply）。换会话后仍以本文「下一份」为准。
>
> 分支：原工作分支 `docs/recalibrate-design`（从 `main` @ `abdc596` 分出）。**2026-08-18 已快进合并进本地 `main`**（`58c0d7b`、`fcdeec2`）。后续以 `main` 上的本文为准；该工作分支可删。
> 开始：2026-08-18
> 范围：按模块推进。**每个模块先校准文档，用户确认后再改该模块代码**，然后才进入下一模块文档。不要把全部文档改完再统一写代码。跨模块缺口记在被依赖的那一模块，不在 01 里顺手改 03/05。
> **外部项目**：记忆后端（09）的 REMS3 fork 正在 `C:\Users\40575\Desktop\prog\Jshi_memory` 独立开发（2026-08-19 建立，基线 `5699c36`），见 `doc/rems3-参考笔记.md`；本仓库不直接改 REMS3 源码。

## 本轮原则

1. 用户意见是候选，先审查再写入正文。
2. 稳定原则仍以 00 第 8 节为准，不因基线实验改。
3. **本轮上下文 = 03 组装全体**（输入、对象、身份、个人世界、回忆、上一活跃区）。活跃区是按 05 缩减/聚焦方案编好的下一份工作上下文（可含回忆摘录），由 16 保存。本轮原话先走 `input_text`，不在认知开始前并入上一活跃区。00 / 02 / 16 / 概念词汇已按 05 回写（D-006）。
4. 主链路对外契约是 `response_plan`，不另落 thought。`wait` 结束本轮活动。
5. 事件只在 09 内部；主链路与 02 / 03 / 05 / 07 暂不使用。归属判断已废弃。反思是占位，不与记忆写成一条后台过程。`concern` / 主体面未完成现实已废除（D-007），不以新名字复活。
6. 《想法记录》I-001–I-005 本轮不纳入契约。I-006 已写入 02 / 00：初次与复位走同一条主链路。
7. 名称与别名字段都必须带（label 非空，aliases 可暂空）；不重名按正常处理；重名必须消歧，不得任取。

## 梳理顺序

按编号走，缺号的跳过并在表里标明。每个模块：文档 →（确认）→ 本模块代码 → 下一模块。00 无独立代码包，只改文档。

主链路文档仍按 00、16、02、03、04、05、06、10、07、30 口径一致；**10 文档与代码已做（按指示跳过 09），下一份仍是 09 文档**。

| 序号 | 文档 | 进度 | 备注 |
|------|------|------|------|
| 1 | 00-流程总览与输入边界 | 已改 | 按 05 回写活跃区；事件仅 09；记忆批次须分型 |
| 2 | 01-身份识别 | 文档已改；代码已做 | 置信度新档位；候选带 aliases；object_id 按已绑定；重名须显著领先 |
| 3 | 02-活跃区与事件装载 | 文档已改；代码已做 | 读上一份活跃区；I-006；`load(subject_id)`；文件名未改 |
| 4 | 03-状态组装 | 文档已改；代码已做 | 五源对接；对象三件套；无认识状态源；窗口源可带回忆摘录 |
| 5 | 04-活动 | 文档已改；代码已做 | 无 waiting；无 active_concern_ids |
| 6 | 05-认知与追加召回 | 文档已改；代码已做 | 说话人单列；缩减/聚焦方案进响应；本轮按方案编活跃区 |
| 7 | 06-认识状态 | 文档已改；代码已做 | 只标 `response_plan`；不碰经历/活跃区/记忆；文件名未改 |
| 8 | 07-活动收尾 | 文档已改；代码已做 | 一律 `completed`；handoff 交编排层调 30；内部活动也走 07 |
| 9 | 08-个人世界 | 文档已改；代码已做 | 薄壳：合并/去重/按等级取量；不装 concern；09 不是子模块 |
| 10 | 09-记忆系统 | 待读 | 接口是否仍偏 `ingest_activity` |
| 11 | 10-行动与反馈 | 文档已改；代码已做 | 语言记录 + 独立语音占位；与 embodied 分口；09 未改 |
| 12 | 13-匠石形态与载体接口 | 待抽查 | 占位，只查与七阶段冲突 |
| 13 | 15-人工介入 | 待抽查 | 占位 |
| 14 | 16-经历账本 | 文档已改；随 05 改了代码 | 保存活跃区（含回忆摘录）；按方案编排；批次分型仍归 30 |
| 15 | 30-记忆数据管控 | 待读 | 批次构造仍在 16 上的债务要写清 |
| 16 | 100-价值观与边界 | 待抽查 | |
| 17 | 独立评价系统 | 待抽查 | |
| — | 11 / 12 / 14 | 无正文 | 不编造文档 |

设计目录外、但会被旧口径带偏的，记在下面「待更改」，不插入上表冒充模块正文。

---

## 已更改

相对本分支起点 `abdc596`：模块正文从 00 开始改。`main` 已接线的七阶段仍继承，避免重做。

### 本分支已改文档

- 00：本轮上下文 = 组装全体；活跃区 = 方案编排结果（可含回忆摘录）；事件只在 09；记忆批次须分型。Cursor 规则已同步。D-006。
- 01：`object_id` 只作关联键；名字=`label`，称呼=`aliases`；确认不再经认识状态机。置信度：渠道已绑定最高，载体相应提高，名字匹配不是最高。
- 02：读取上一份活跃区，不是「仅经历窗口」；初次/复位同一 `load`。文件名仍含「事件装载」。
- 03：五源按已公布端口对接；本轮输入是刺激不是装载源；对象源消费 `SpeakerCandidate`；不注册认识状态源。窗口源可带出活跃区里的回忆摘录。个人世界源跳过活跃区已有的 `personal:{id}`。
- 04：活动是一轮工作单元；正文不再写事件；状态无 `waiting`；代码已去 `active_concern_ids`。
- 05：本轮上下文=组装全体；心智必出回应计划与缩减/聚焦方案；不再调模型即编新活跃区；记忆批次须分型；05 不 ingest。代码：`ModelRequest.speaker`、可寻址 context、`context_assessment` 进响应；最终认知后 16 编排。
- 06：只校验并标记 `response_plan`；不消费召回/对象确认/缩减聚焦；不写经历、不改活跃区、不投递记忆。`wait` 是回复方式。文件名未改。代码抽到 `src/jshi/responsemark/`。
- 07：一律 `completed`（含 `wait`）；不写经历、不改活跃区、不投递记忆、不处理事件。编排层凭 `handoff_to_memory_control` 调 30。内部活动也走 07。
- 08：薄壳。子模块交有序列表；08 按 id 去重、按等级取量。条目加 `level` / `entry_type`（类型占位）。不装 `concern`。03 跳过活跃区已有的 `personal:{id}`。
- 10：有 verbal 则落 `language_action` 并调用独立 `SpeechPort` 占位；embodied 仍走机器人口。语音不是新的计划通道。
- 《概念词汇》：活跃区 / 本轮上下文 / ContextViewState 已按 05 回写；个人世界装载系统已按 08 回写。D-007：未完成现实 / 关切 / `concern` 改为已废除条；事件改为 09 记忆单元。

### 主链路（代码已接线）

- ① 对象解析与落位；16 `append_external`（先入账、待合并）
- ② 02 只读上一份活跃区（不调模型、不重切）
- ③ 组装：主体身份 / 对象摘要 / 上一活跃区 / 08 / 09 线索；本轮原话在 `input_text`
- ④ 建活动（一轮一活动；`wait` 仍关闭）
- ⑤ `response_plan` + 缩减/聚焦方案；最多一轮追加召回；不落 thought
- 06 校验并标记 `response_plan`（不碰经历/活跃区/记忆）
- ⑥ 10 分发；verbal 落记录并走独立语音占位；embodied 走肢体；之后 16 按方案编新活跃区
- ⑦ 07 一律 `completed`（含 `wait`）→ 30 `run_once`（批次分型尚未落地）

### 文档（main 已对齐过一轮）

- 00 / 总体架构 / 概念词汇 / 协作进度 / 主体过程实验 / 验证章程 V7：七阶段 + 无归属 + 无 thought
- 02 / 03 / 04 / 05 / 06 / 07 / 08 / 10 设计标为定稿骨架；09、16、30 等多为初稿

这些「已对齐」里仍混有错误口径（本轮输入 ≠ 活跃区），见待更改。

---

## 待更改

### A. 本轮必改（设计口径）

- [x] 00：按 05 回写活跃区（编排后的工作上下文，可含回忆摘录）
- [x] 02：按 05 回写（读上一份活跃区，可含回忆摘录）
- [x] 概念词汇「活跃区 / 上下文视图 / 本轮上下文」— 按 05 回写
- [x] 03：工作集含 `input_text`；与 01/02/08/09 端口对齐，不另造检索
- [x] 16：保存新活跃区；执行 05 方案且不再调模型；待合并是记账
- [ ] 主体过程实验、验证章程 V7、《总体架构》里旧活跃区句子 — 抽查时改
- [x] D-007：废除 concern / 主体面未完成现实；V4、V6 不作缓验
- [ ] 16：活跃区程序侧硬上限（规则先写清；不要用第二套 FIFO 与 05 方案抢裁剪权）
- [ ] 回忆摘录：权威在 09，活跃区只缓存引用；读取时校验，不由 09 回写 16

### B. 本轮读时处理（可能改、可能只记）

- [x] 04：`waiting` 不是活动状态；`wait` 关闭本轮，下一轮新活动。07 已对齐为一律 `completed`
- [x] 01：对象确认写在⑤，不再经认识状态机；下游须带名字与称呼
- [x] 05 文档：缩减/聚焦为本轮必出方案；代码已接 `context_assessment`（Echo / OpenAI 适配器仍只出空方案）
- [ ] 06 文件名仍为「认识状态」（本轮不改；契约已是活动回复标记）
- [ ] 01：deny 后的恢复/纠错路径（待验证，本期不改 1.00）
- [ ] 09：`ingest_batch` vs `ingest_activity`
- [ ] 30：`build_memory_batch` 仍在 16

### 当前模块代码（10，已做）

- [x] `SpeechPort.speak` 占位，与 `RobotActionPort` 分立
- [x] 有 verbal 则 `language_action` 与语音口同开；wait/think/ignore 不发音
- 对应测试：`tests/test_support_modules.py`、`tests/test_response_plan_flow.py`

### 已完成模块代码（08）

- [x] 薄壳合并子模块有序列表；跨模块按 id 去重；按 `load_level` 取量
- [x] 条目字段 `level` / `entry_type`；不装 `concern`
- [x] 03 跳过活跃区已有的 `personal:{id}`
- 100 增加 `list_ordered`（不按 query 截断）；承诺/其余种类用占位适配器
- 对应测试：`tests/test_personal_world_port.py`

### 已完成模块代码（07）

- [x] 主链路一律 `completed`；已关闭则幂等，不再 handoff
- [x] `transition_id` 回传；编排层凭 `handoff_to_memory_control` 调 30
- [x] 内部活动 `reflect` 改走 07，不再自己写关闭
- [x] 关闭不写经历、不改活跃区
- 对应测试：`tests/test_activity_close.py`、`tests/test_activity_lifecycle.py`

### 已完成模块代码（06）

- [x] 校验抽到 `src/jshi/responsemark/`；`process` 只调用端口
- [x] 静默 mode 带 `verbal`、未知值忽略；`respond` 无 item、静默无 reason 记入审计
- [x] 标记不写经历、不改活跃区、不关闭活动
- 对应测试：`tests/test_response_mark.py`、`tests/test_response_plan_flow.py`

### 已完成模块代码（05）

- [x] `ModelRequest` 单列 speaker，context 条目带 id / source
- [x] `ModelResponse.context_assessment`；去掉 `need_recall`；召回只走 `recall_requests`
- [x] 最终认知后 16 按方案编活跃区；补召回循环里不裁；对象引用受保护
- 记忆投递分型归 30，不在 05 实现
- 对应测试：`tests/test_cognition_recall.py`、`tests/test_experience_ledger.py`

### 已完成模块代码（04）

- [x] `ActivityStatus` 去掉 `waiting`
- [x] 去掉 `active_concern_ids`
- 主链路关闭已是 `completed`；读旧库若遇 `waiting` 按 `completed` 解释
- 对应测试：`tests/test_activity_lifecycle.py`

### C. 留给后续模块

- [ ] `方案(coding)/02`、`03` 仍写 `ActiveZoneView` 与窗口游标
- [ ] `tests/README.md` 仍写阶段 0–6、活跃区剔除/召回
- [x] `process.py` 过时注释：「事件挂载在认知后由模型聚焦回填」
- [x] 05 `ModelRequest` 已单列说话人；响应带 `context_assessment`（真实适配器尚未解析结构化段）
- [x] 09 召回结构仍只有 `object_id`；展示名由 03 join，不改 09 过滤键
- [ ] 记忆批次须分型（`new_input` / `new_response` / `in_context_recalls`）——归 30
- [ ] 记忆批次构造迁到 30
- [ ] 10 语音与肢体的真实实现（13）；结果反馈；11–15 实现
- [x] `.pytest-tmp*` 已写入 `.gitignore`

### D. 本轮新发现

- 00 → 侧流「认知后显式建事件」含混 → 改为事件只在 09，别处暂不用 → 已改
- 00 → 「原认识状态机暂缓」重复提已删除的设计 → 删干净 → 已改
- 00 → 记忆整理、反思、学习、治理捆成一条后台升格 → 拆开；12 占位 → 已改
- 00 → 图上「结果反馈触发反思」「认知创建事件写入个人世界」 → 已删线

- 01 → 确认仍写「考虑中 / 认识状态迁移」→ 改为 05 `object_assessment` 回填档案 → 已改
- 01 → 下游只传 id，模型看不清是谁 → 规定须带 label/aliases；03 对象源已装三件套；`ModelRequest` 字段仍归 05
- 01 → 文字带对象应最高、声纹相应提高 → 渠道已绑定与正文名字分开；已确认文字绑定 1.00，载体略低 → 已写入 01，代码未改

- 01 → 名称与别名必须带；不重名正常；重名记忆消歧 → 已写入 §1.1；首次 aliases 可空；代码未改

- 02 → 文档把 02 写成整块活跃区 → 改为只交既往视图；本轮输入归 03 → 已改
- 02 → `load` 仍收 `input_text` / `limit_chars` 却丢弃 → 签名改为只收 `subject_id` → 已改
- 02 → 初次/复位是否另开流程 → 采纳 I-006，02 无单独复位接口 → 已改

- 03 → 身份源误标为 01、对象源空、认识状态源仍注册 → 五源对接；主体身份 ≠ 说话人；不再二次 resolve/load → 文档与代码已改
- 03 → 对 08/09 再按条数截断、跨源裸 id 去重 → 预算归源模块；id 带前缀、源内去重 → 文档与代码已改

- 04 → 正文用「不挂载事件」维持已删除的结构 → 删干净，只写活动是什么 → 已改
- 04 → `waiting` 与 `wait` 并存 → 活动状态无 waiting；wait 是 06 回复方式 → 文档与代码已改；07 已对齐一律 `completed`

- 05 → 上下文被写成仅经历窗口；trim/focus 写成「本轮不执行」→ 改为组装全体；方案必出；不调模型即编新活跃区 → 文档已做
- 05 → 记忆「策略包」无标记交给 09 → 必须分型，09 不再判别输入/回忆 → 文档已做
- 05 → 对象可从请求里省略 → 单列说话人；缩减不得拿掉对象 → 文档与代码已做
- 00 / 02 / 16 / 概念词汇 → 按 05 回写活跃区 → 文档已做；16 代码随 05 增加回忆摘录与对象保护

- 06 → 「靠 `ContextViewState` 接上」与「消费上下文补充」过时 → 连续性靠活跃区；06 只消费 `response_plan` → 文档与代码已改
- 06 → 文件名仍为「认识状态」→ 本轮不改；契约按 D-005 是活动回复标记

- 07 → 收尾结局写成完成/等待/放弃 → 主链路一律 `completed`；`wait` 不是活动状态；投递仍归 30 → 文档与代码已改
- 07 → 内部活动自己关闭 → `reflect` 改走 `ActivityClosePort`

- 08 → 自己排序、按 query 筛选、把 09 和 concern 算进个人世界 → 改为薄壳；concern 过时不装、不迁 07 → 文档与代码已改
- 08 → 价值承诺前缀短期内重复叠入 → 08 按 id 去重；03 跳过活跃区已有 `personal:{id}`

- 10 → 只落文字记录、语音混在机器人接口 → 文本要说出来：独立 `SpeechPort` 占位，与 embodied 分口 → 文档与代码已改；09 未动

- 审查 → 活跃区无硬上限 → 诊断成立；不采用静默 FIFO 与 05 抢裁剪；规则记入 16
- 审查 → concern 只写不读、V4 无法验 → 诊断成立；不装回 08。D-007：概念废除，V4/V6 不作缓验
- 审查 → 回忆摘录双源 → 诊断成立；按 D-006：引用为准、读取校验，09 不回写 16
- 审查 → 绑定 1.00 与 deny 永挡 → 1.00 是档位不是概率；deny 缺恢复路径成立，列为 01 待验证

---

## 变更日志

| 日期 | 改了什么 | 状态 |
|------|----------|------|
| 2026-08-18 | 开分支 `docs/recalibrate-design`，立本记事 | 已做 |
| 2026-08-18 | 增加 Cursor 规则 `.cursor/rules/本次设计梳理.mdc` | 已做 |
| 2026-08-18 | 重校 `doc/design/00-流程总览与输入边界.md` 与 00 规则 | 已做 |
| 2026-08-18 | 重校 `doc/design/01-身份识别.md` | 已做 |
| 2026-08-18 | 01 置信度：渠道已绑定最高，载体相应提高 | 文档已做 |
| 2026-08-18 | 约定：按模块，先文档后该模块代码 | 已做 |
| 2026-08-18 | 01 §1.1 名称/别名必带；不重名正常；重名消歧 | 已做 |
| 2026-08-18 | 01 代码：置信度档位、aliases、object_id 绑定、重名显著领先 | 已做 |
| 2026-08-18 | 想法 I-006：初次使用与复位后走同一条主链路 | 已采纳（02 / 00） |
| 2026-08-18 | 重校 02 文档与代码：只交既往视图；`load(subject_id)` | 已做 |
| 2026-08-18 | 重校 03 文档：按 01/02/08/09 与身份系统端口对接 | 已做 |
| 2026-08-18 | 03 代码：五源对接、对象三件套、去掉认识状态源 | 已做 |
| 2026-08-18 | 重校 04 文档：不写事件；活动无 waiting | 已做 |
| 2026-08-18 | 01–03 正文去掉「不装事件」对照句 | 已做 |
| 2026-08-18 | 04 代码：去掉 waiting 与 active_concern_ids | 已做 |
| 2026-08-18 | 重校 05 文档：上下文=组装全体；缩减/聚焦必出；记忆批次分型 | 已做 |
| 2026-08-18 | D-006：活跃区是方案编排后的工作上下文 | 已做 |
| 2026-08-18 | 回写 00 / 02 / 16 / 概念词汇活跃区口径 | 已做 |
| 2026-08-18 | 05 代码：说话人、可寻址上下文、按方案编活跃区；16 回忆摘录 | 已做 |
| 2026-08-18 | 重校 06 文档：只标 `response_plan`；不碰经历/活跃区/记忆 | 已做 |
| 2026-08-18 | 06 代码：`responsemark` 端口；标记不写经历、不改活跃区 | 已做 |
| 2026-08-18 | 重校 07 文档：一律 `completed`；不碰经历/活跃区/记忆/事件 | 已做 |
| 2026-08-18 | 07 代码：幂等关闭、handoff 调 30、内部活动走 07 | 已做 |
| 2026-08-18 | 重校 08 文档：薄壳、等级取量、不装 concern | 已做 |
| 2026-08-18 | 08 代码：合并去重取量；03 跳过已有 personal id | 已做 |
| 2026-08-18 | 重校 10 文档：语音占位独立于 embodied | 已做 |
| 2026-08-18 | 10 代码：`SpeechPort`；verbal 落记录并发音占位 | 已做 |
| 2026-08-19 | D-007：废除 concern / 主体面未完成现实；回写总图、词汇、验证章程、00 第 4 条 | 已做 |

下一份：**09 文档**（本轮按指示先改了 10，09 仍待读）。重校 09 时不得把未完成事件写成主体面缓冲。30 批次分型尚未落地。Echo / OpenAI 适配器仍只出空缩减/聚焦方案。
