# 方案（coding）

本目录存放每个模块**从设计文档到代码实现之间**的实现方案。方案是"文档"和"代码"之间的过渡产物：文档回答"系统应该是什么"，方案回答"这次代码具体怎么落地"。

协作流程（文档 → 方案 → 代码 → 测试 → 提交）见 [`doc/协作流程.md`](../doc/协作流程.md)。

## 工作流

```text
全局与依赖检查 → 文档（doc/design/）→ 必要时验证性实验
→ 方案（本目录）→ 代码（src/）→ 测试与验证（tests/）
```

1. 全局与依赖检查：确认项目目标、稳定原则、相邻模块契约和待验证问题；**查看《想法记录》**，评估相关条目是否纳入本轮；
2. 设计文档定稿：`doc/design/0X-*.md`（先讨论完善，再定稿）；
3. 关键不确定性先做验证性实验，结论进入决策记录，不直接把实验代码当正式实现；
4. 编写实现方案：本目录下每模块一份，明确实现范围、接口落点、数据结构、测试计划和回退；
5. 按方案实现代码：系统占位包 + 主流程接线；
6. 按方案补模块测试和流程回归；核心假设另按《第一阶段验证章程》检查。

## 目录结构

```text
方案(coding)/
  README.md          # 本说明（工作流 / 模板 / 状态）
  00-流程总览.md      # 整体实现顺序与依赖（可选）
  01-身份识别.md      # 每模块一份，编号与 doc/design 对应
  02-活跃区与事件装载.md
  09-rems3-适配器.md   # 可选同进程后端；已确认并编码
  ...
```

## 方案模板（每份方案包含）

1. **目标与范围**——本次实现到什么程度（占位 / 最小 / 完整）；
2. **接口落点**——对应 `port.py`、数据类、常量，新增或修改哪些；
3. **数据结构**——新增字段、表、事件类型、状态；
4. **行为变更**——主流程如何接线、记录什么历史事件、有什么约束；
5. **测试计划**——新增 / 修改哪些测试用例，对应文档中的验收标准；
6. **风险与未决**——占位部分、未来接入点、可能的坑；
7. **记录、评价与参数影响**——本方案新增/修改哪些记录点、评价点、可调参数及调节方（14），对应 00 总览表。

## 状态跟踪

| 模块 | 设计状态 | 方案状态 | 实现状态 | 验证状态 |
|------|----------|----------|----------|----------|
| 00 流程总览与输入边界 | 定稿（骨架） | 简版（实现顺序） | 原型接线（ContextView + `response_plan`） | 结构通过 |
| 01 身份识别 | 校准中 | 初稿 | 原型 | 结构通过 |
| 02 活跃区与活动窗口 | 定稿 | 初稿 | 最小：只读 16 的 `ContextViewState` | 结构通过 |
| 03 状态组装 | 初稿 | 初稿 | 最小：6 源，不装载事件 | 结构通过* |
| 04 活动 | 初稿 | 初稿 | 最小：生命周期记账；不挂载事件 | 结构通过 |
| 05 认知与上下文统筹 | 校准中（D-008） | 已确认（`05-取消补召回与有效性分析.md`） | 一次认知；打分入库；规则分析器；策略快照 | 结构通过 |
| 06 活动状态标示 | 定稿 | 初稿 | 最小：校验并标记 `response_plan` | 结构通过 |
| 07 活动收尾 | 初稿 | 初稿 | 最小：一律 `completed`（含 `wait`）；不含记忆逻辑 | 结构通过 |
| 08 个人世界 | 初稿 | 初稿 | 薄壳 + 100 装载侧 | 结构通过 |
| 09 记忆系统 | 定稿（契约） | 进程内方案已落地 | 最小：`ingest_batch` + 召回 | 结构通过 |
| 09 REMS3 适配器 | 契约定稿（§10） | 已确认（`09-rems3-适配器.md`） | 最小：同进程薄适配器 + 工厂；默认进程内 | 互转与工厂结构通过；live rems 默认 skip |
| 16 经历账本 | 初稿 | 初稿 | 最小：`ContextViewState`；`build_memory_batch` 仍暂留 | 结构通过 |
| 30 记忆数据管控 | 初稿 | 初稿 | 最小：07 之后 `run_once`；批次构造仍调 16 | 结构通过 |
| 10 行动与反馈 | 初稿 | 初稿 | 最小：语言已记；机器人/结果反馈占位 | 结构部分通过 |
| 11 治理与连续性 | 待补初稿 | - | 占位原型 | 未验证 |
| 12（已迁） | 指针 | - | 正文见 300 | — |
| 300 自省 | 初稿待审阅（`doc/design/300-自省.md`） | 初稿（`300-自省.md`；本次最小：事件结果 + 闲时回顾） | 占位雏形 `reflect` | 待跑 |
| 13 匠石形态 | 初稿 | - | 未实现（挂靠点预留） | 未验证 |
| 14 统计分析与参数调优 | 初稿（占位） | - | 未实现（挂靠点预留） | 未验证 |
| 15 人工介入 | 占位初稿 | - | 未实现（挂靠点预留） | 未验证 |
| 100 价值观与边界 | 校准中 | 初稿 | 完整最小实现 | 结构通过，行为未验证 |
| CLI 对话界面 | 无独立 design 编号；用法见 CLI使用说明 | 已确认（`cli-对话界面.md`） | 最小（本分支 `cli/talk-shell`） | 会话与简易循环结构通过 |
| 16 经历账本落盘 | 16 已写落盘；I-010 落盘切片已采纳 | 已确认（`16-经历账本落盘.md`） | 最小：同库 JSON 载荷 + CLI 注入 | 待跑测试 |
| 05 问第三人召回 | 05 / 01 已补；I-011 已采纳 | 已确认（`05-问第三人召回.md`） | 最小：skill v4 + 只读 lookup + 召回补 id | 结构通过 |
| CLI 耗时与折行 | 无独立 design；I-012 / I-013 已采纳 | 已确认（`cli-耗时与折行.md`） | 最小：旁路 `/timing` + Log/plain 折行 | 结构通过 |
| 工具使用 | 初稿已按 I-017 / I-018 改口径 | 通道与全案已确认（`工具使用.md` / `200.md`） | 三口 + `tool_input`；两 skill；210 账本；边写入账 | 结构通过 |
| 05 模型提示词分层 | 05 §4 已补 HTTP 形态 | 已确认（`05-模型提示词分层.md`） | 最小：system/user 分层；skill v8 | 结构通过 |
| 05 提示词风格与活跃区全改 | 05 / 16 / D-009 | 已确认并编码 | 风格包 + 整份现场 + 召回流水 | 结构通过 |
| 16/30 回忆对象绑定 | 00 §4.1 / 16 / 30 / 09 写入侧已改文档 | 已确认并编码（`16-30-回忆对象绑定.md`） | 最小：落位补 objects、回复/状态同带、30 填 interlocutor | 结构通过 |

> 设计、实现和验证分别标记。结构通过表示当前接口与记录可运行；行为通过表示跨活动中产生预期影响；对照通过表示相对备选有证据。
>
> \* 03：实现已不把事件装进活跃区；`tests/test_external_activity_flow.py` 与 `tests/test_subject_process.py` 中仍有 4 例按旧「关切/事件进组装」断言，尚未改测试。
>
> 主链路不落 thought。`ModelResponse` 尚未接 `context_assessment`（缩减/聚焦未由模型驱动）。记忆游标与批次构造设计上属 30，代码仍在 16。

## 约定

- 方案编号与 `doc/design/` 一一对应；
- 一个模块只实现一次"占位 → 最小 → 完整"的升级路径，方案里写明本次做到哪一级；
- 方案定稿后再动代码；代码实现后按方案的测试计划验证；
- 已有代码而设计缺失的模块，先将代码视为原型证据，反向整理设计初稿，但不得把当前行为自动写成规范；
- 01 已有代码本轮不改实现细节，只按校准后的输入与对象契约复核；
- 02 已定稿为只读 `ContextViewState`；03 不装载事件。后续改动须同步 16 与 00。
