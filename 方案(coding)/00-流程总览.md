# 00 流程总览——整体实现顺序与依赖

> 对应文档：doc/design/00-流程总览与输入边界.md（定稿骨架）
> 定位：00 不映射到单个代码系统，本文件只定**整体实现顺序与依赖**。

## 1. 现状

外部主链路已按七阶段接线，并插入 06 标记与 16 视图维护：

```text
① 对象解析 + 落位 + 16 append_external
② 02 只读 ContextViewState（不调模型、不重切）
③ 03 组装（身份 + 视图 + 个人世界 + 记忆；不装载事件）
④ 04 建立活动（不挂载事件）
⑤ 05 产出 response_plan（可追加一轮召回；不落 thought）
   → 06 校验并标记
⑥ 10 按计划分发；respond 后 16 apply_context_assessment
⑦ 07 关闭为 completed（含 wait）→ 30 run_once
```

归属判断已废弃，不在主流程中。活跃区不是事件区。对外契约是 `response_plan`。认识状态机暂缓（D-005）。

已有接线不等于跨活动行为已经验证，也不等于 03/07/09/10/16/30 设计已定稿。

## 2. 依赖关系

```text
16 经历账本（原文流 + ContextViewState）
    ↑ ① 追加外部经历          ↑ ⑥ 追加回复/状态并更新视图
    ↓ ② 只读                   ↓ 30 读批次（当前仍调 16.build_memory_batch）

01 身份识别 ──→ ① 落位
02 活跃区 ──依赖 16 ──→ 03 状态组装 ──→ 04 活动 ──→ 05 认知
09 记忆 ──装载/追加召回──→ 03 / 05（05 只提候选，主流程调 09）
05 ──→ 06 标记 ──→ 10 行动
10 / 07 ──→ 16
07 ──→ 30 ──→ 09 ingest_batch（不得改 ContextViewState）
08 / 100 ──装载──→ 03
11 / 12 / 13 / 14 / 15 ──占位或未实现，不阻塞当前主链
```

## 3. 当前不宜当作“下一步必做”的旧计划

下列旧顺序已经过时，不要再按其推进：

- 先写 02 方案、从主流程摘除归属判断（归属已摘除，02 已只读视图）；
- 03 按「活跃区事件 + 关切中心 / 全量」双路径组装（事件属 09，组装不装事件）；
- 07 兼管事件生命周期与记忆投递（事件在 09，投递在 30）。

## 4. 已知实现债务（未排期）

- `ModelResponse` 尚未接 `context_assessment`；
- 记忆批次构造与 `memory_start` 应从 16 迁到 30；
- 4 个旧测试仍期望组装装载事件/关切；
- 09 接口文档仍偏 `ingest_activity`；
- 10 机器人与结果反馈、11–15、流式输入（I-001）本阶段不铺开。

## 5. 状态

见 `README.md` 状态跟踪表；本文件随整体进度更新。
