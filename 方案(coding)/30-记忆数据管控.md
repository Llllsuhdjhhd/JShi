# 30 记忆数据管控——实现方案

> 对应文档：doc/design/30-记忆数据管控.md（初稿）
> 状态：初稿，按已接线代码整理

## 1. 目标与范围

本次做到**最小**：活动收尾后尝试投递一批经历数据给 09。

- `evaluate`：上次未结束、失败达重试上限、或数据不足 → 不触发；
- `run_once`：构造 `MemoryBatch`，调用 09 `ingest_batch`，成功则推进游标；
- 活动不等待记忆（I-005：主流程只调用一次，不根据评价改历史）。

阈值目前为代码占位常量，不在主流程里实时计算。I-002 统一评价系统不纳入本期。

## 2. 接口落点

- `src/jshi/memorycontrol/port.py`
- `src/jshi/memorycontrol/inprocess.py`
- `src/jshi/subject/process.py`：`experience()` 在 07 close 之后调用 `run_once`
- 经历流：`ExperienceLedgerPort.build_memory_batch`

## 3. 行为变更

07 完成后主流程调用 30。30 不阻塞下一轮活动。失败保留批次、允许重试，不丢原文。

## 4. 测试计划

- `tests/test_memory_control.py`：不足不触发、成功推进、失败可重试；
- 主流程测试：收尾后有 `memory_control_attempted` 评价事件。

## 5. 风险与未决

- 方案 README 曾写“未接主流程”，代码已接，以代码为准；
- 阈值、14 调参、独立评价旁路尚未接上。

## 6. 记录、评价与参数影响

- 记录点：记忆投递台账、`memory_control_attempted`（评价旁路）；
- 评价点：投递成功率、重试次数，供未来 14 使用，不回写主体状态；
- 参数：`min_chars` / `min_segments` / `max_age_seconds` / `max_retry`，默认占位，未来由 14 调。
