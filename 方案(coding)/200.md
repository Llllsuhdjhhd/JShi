# 200 工具使用——实现方案

> 对应文档：`doc/design/工具使用.md`（I-017 / I-018）。
> 前一刀通道方案：`方案(coding)/工具使用.md`（I-017 门面，已编码）。
> 状态：已确认并编码。设计已按此回写；§9 三刀已落地。

## 0. 何处用模型，是否都做成 skill

**200 自己要思考的，做成 skill。不思考的，不要做成 skill。引擎内部的模型，不是匠石 skill。**

| 位置 | 调模型？ | 形式 | 理由 |
|------|----------|------|------|
| 05 回复 / 写场 | 是 | 已有 `CognitionSkill` / `WriteZoneSkill` | 不属 200。05 只出 `use_tool` + `need`。 |
| **205 策划** | 是 | **新 `ToolPlanSkill`** | 选模板、填参数、写估价槽。规则 echo 只是占位。 |
| **包装可见人话** | 是 | **新 `ToolWrapSkill`** | 把 210 里的阶段性事实写成给 03 的一句。 |
| 「要不要可见」单独一次 | 否 | 包进 `ToolWrapSkill` 的 `visible` | 再开一个 skill 会和包装抢活，也多一次延迟。 |
| 210 记挂 | 否 | 账本 | 多本并行；不思考。 |
| 206 适配 | 否 | 翻译 | 读 Pi 事件、写入对应 210。不改 Pi 内核。 |
| Pi 进程里的 LLM | 是，但是引擎 | **不是**匠石 skill | 改 Pi 要单独评估风险收益，本方案不做。 |
| CLI `jshi tool` | 否（旁路） | 直打引擎 | 调试用，不经 200 skill。 |

两条 200 skill 都走现有 `Skill` 基类（instruction + JSON Schema + 解析 + 降级 + `model_tag`），在 `skills.json` 注册。不要复用认知 skill，也不要为 200 另写一套调模型框架。

思考档位：这两条是填槽/写一句人话，`thinking` 默认 `disabled`，避免 V4 Flash 后台写长思维链。

---

## 1. 目标与范围

**本次方案：200 内部按「多交接、多记挂、两 skill」落地的完整切法。** 编码分三刀（§9），确认方案后先改设计再按刀编码。

### 已落地（I-017 通道，不重做）

- 05 只出 `use_tool` / `need`；写场后 `intake`，07 照关。
- `ToolService`：`intake` / `list_visible` / `cancel`。
- 交接 `tool.jsonl` ≠ 记挂 `hang.jsonl`。
- 03 原样装 `summary` → `tool_input`；不叫醒；写场不带这一路。
- `RulePlanner`、Stub 整包 `submit`、Pi 攒到 `agent_settled`：视为占位。

### 本方案要做

1. **210 = 记挂账本**（号留下）。只收 Pi 的阶段性事实，丢掉思维链；一本一请求；有新事实就通知 200。
2. **`ToolWrapSkill`**：只读这一本 210 的新事实，产出 `{visible, summary}`。可见 = 这句话可进 `list_visible`。
3. **`ToolPlanSkill`**：读交接 + 模板说明（含时间、异常），产出定义 / 失败句。失败不建 210。
4. **206**：边读边写入对应 210。不改 Pi 协议、不加 Pi 字段。修适配器侧明显坑（如 `stderr=PIPE`）。
5. **多本并行**：可重入；一本一包装队列；按对象 `list_visible` 上限 8；后一句覆盖给 03 看的那条。

### 不做

- 改 Pi 内核或 Pi RPC 加 `time` / `exception` 字段。
- 叫醒 / 插内部活动。
- 05 填模板名或参数。
- 200 经 10 对人开口。
- 自省 300 的完整入口（`origin=introspection` 字段预留即可）。
- `fact/tool_*`、08、权限 100/15 实装。

---

## 2. 内部结构与数据流

```text
05 need → 200.intake（交接，立刻返回）
              ↓
         ToolPlanSkill（205）
              ├ 失败 → list_visible 交失败句（无 210）
              └ 成功 → ToolRequest + estimate 槽
                         ↓
                    建 210（一本）
                         ↓
                    206 ↔ 该次 Pi 会话
                         ↓
                    事件写入这一本 210（不要思维链）
                         ↓
                    通知 200：这本有新阶段性事实
                         ↓
                    ToolWrapSkill（只看这一本）
                         ↓
                    写回 210：visible + summary
                         ↓
下一轮 03：list_visible(对象) → tool_input
```

复杂情形（正路，不是边角）：

| 情形 | 处理 |
|------|------|
| 一条 need 拆多条请求 | 一本交接，多本 210，各跟各的 Pi。 |
| 上一轮还在跑，又 intake | 旧账继续，新账另建；不得一把锁堵住第二次 `intake`。 |
| 同一对象多条可见 | 只交该对象、`visible` 且有 `summary` 的，上限 8。 |
| 不同对象 | 按 `object_id` 隔离。 |
| 一本上多次阶段性成果 | 新 `summary` 覆盖给 03 看的那条；同一 `task_id` 03 只装一次；旧句留账备查。 |
| 包装失败 | 跟随不停；本轮可不交可见，或降级极短事实句；禁止 Pi 原文进 03。 |
| 取消 | 只注销那本 210 及其会话；其它照跑。 |
| 进程崩溃 | 210 行还在，Pi 子进程可丢；不假装跑完。 |

**一本一队列**：包装调模型排在该 210 自己的队列，不挡 206 读流，也不跟其它本抢一把全局模型锁到无法 `intake`。允许有总并发上限（占位，14 后调），排队而不是丢掉事实。

---

## 3. Skill 简版

共用约定：输入先做成窄结构，再塞进 `ModelRequest.input_text`（JSON 文本），走 `Skill` 基类。结果是候选。失败走降级，不把模型原文当人话。`model_tag` 记在该次交接或该本 210 的 meta。

### 3.1 `ToolPlanSkill`（205）

- **name**：`tool_plan`；**version**：`v2`（检索循环）
- **何时**：`intake` 之后、建 210 之前。每份交接一次（拆多条请求则一次输出数组，或连续几次调用；本刀允许一次输出 1 条，多条为后续）。
- **读**：交接（`need`、口头、对象、活动、`field_ref`）+ **按 `field_ref` 现读的现场正文**（木头活跃区 / 人格片场；读不到则空）+ 匠石侧模板目录（槽位说明：意图、预估时间、费用、可能结果、风险/异常、参数、期望形态）+ 引擎目录（小则整表；大则分层披露，模型 `lookup.search` / `describe` 后再点名。见 `205-工具检索.md`）。
- **不读**：05 提示词、人格风格说明、`response_plan` 全文、Pi 思维链、09 记忆。现场全文**不写入**交接账本。205 **不执行**工具。
- **instruction（简）**：

```text
你是匠石的工具策划，不是对说话的那一轮认知。
根据意图选一个定义模板，填参数和估价槽。
不要点名编造目录里没有的模板。
外部路径已经对人说过需求的，定义须与那句一致，不要另起一套。
填不出就失败，写出一句给匠石自己看的原因，不要装成已经在跑。
```

- **schema（简）**：

```text
ok: bool
template: string          # ok 时；匠石侧模板名
params: object
estimate: { time_est_ms, cost_est, benefit, downside, need_confirm }
expected_result: string
ask: execute | propose_only   # 主路径默认 execute
error: string             # !ok 时，给 03 的失败句
```

- **降级**：解析失败或 `ok` 假 → `PlanFailure`（现有失败可见路径），不建 210。不要默默 `echo`。
- **落点**：`src/jshi/skill/tool_plan.py`；`plan.py` 的 `ToolPlanner` 换成这个；测里仍可注入 `RulePlanner`。

### 3.2 `ToolWrapSkill`（包装；200 内部，不占对外号）

- **name**：`tool_wrap`；**version**：`v1`
- **何时**：某本 210 新增了**阶段性事实**（有内容的 progress.partial、或 result）。不要每个 JSONL 行都调。Pi 思维链 / 无内容的 start 心跳：不调。
- **读**：该本 `need`、已发出的模板名、**本批新事实**（已剥思维链）、上一句 `summary`（若有）。
- **不读**：其它 210 的账、对方原话、活跃区全文。
- **instruction（简）**：

```text
你把工具进行中的事实写成一句给匠石看的话，像他自己刚弄清的进展。
不是对那个人开口，不要称呼对方、不要写成 response_plan。
不要把引擎思维链、调试日志写进去。
没有可说的阶段性成果就 visible=false。
有可说的就一句，短，不要堆过程。
同一件事不要换个说法再报一遍。
```

- **schema（简）**：

```text
visible: bool
summary: string    # visible 时必填；给 03 原样装的那句
```

- **降级**：解析失败 → `visible=false`（跟随继续）。连续终态仍失败 → 可用结果里已有的极短 `error`/`status` 作一句，仍禁止 Pi 长文。
- **可见**：`visible=true` 且 `summary` 非空，才进 `list_visible`。策划失败条目仍走交接上的 `plan_error`，不经本 skill。
- **落点**：`src/jshi/skill/tool_wrap.py`；由 `ToolService` 在该 210 的包装队列里调用。

`skills.json` 增：

```text
tool_plan: thinking disabled, streaming false
tool_wrap: thinking disabled, streaming false
```

---

## 4. 非 skill 块

### 4.1 210 记挂

号留下。存储仍默认 `{data-dir}/hang.jsonl`，与 `subject.sqlite3` 分开。

做：按 `task_id` 追加反馈；保存当前 `visible` / `summary`；有新阶段性事实则投递包装队列；按对象列出；注销。

不做：选模板、填参数、写人话、对人开口。

`notify_caller` 改名为 `visible`（读旧键兼容一版）。注释里的「叫醒」删掉。

### 4.2 206 与 Pi

`PiEngine` 继续只翻译：发 prompt、按 LF JSONL 读、`tool_execution_*` → 信封。

本方案改适配器行为，不改 Pi：

- 读到一条可入账事件，**立刻** `HangStore.append_feedback`，不要等 `agent_settled` 整包。
- 入账前丢掉思维链类事件（未映射为 start/update/end 的，直接丢）。
- `update.partialResult` 有文本才算阶段性事实，才通知包装。
- `stderr` 不要 `PIPE` 堵死（改继承或文件，属 206）。
- 时间、异常：**不**往 Pi 协议加字段。写在匠石模板说明和（若有）Pi `description`/`SKILL.md` 文本里，给 `ToolPlanSkill` 读。

Stub 仍可整包，但 Runner 应改为「引擎若回调/迭代就逐条入账」；Stub 没有流时，跑完一次写入仍可，包装仍只在有 result/有内容 progress 时触发。

### 4.3 定义模板说明

新增匠石侧模板目录（建议 `src/jshi/tool/templates/*.json` 或一份 `catalog.json`）。每条至少：

```text
name
intent_slot
time_note          # 预估时间怎么填、未知时怎么说
cost_note
result_note
exception_note     # 常见失败 / 异常
params             # 槽名与含义
expected_shape
```

通用底模板一条；`echo` 保留给测试。真 Pi 命令名与匠石模板名的对应由策划 skill 选，不在 05。

---

## 5. 接口落点

| 落点 | 改动 |
|------|------|
| `src/jshi/skill/tool_plan.py` | 新。`ToolPlanSkill`。 |
| `src/jshi/skill/tool_wrap.py` | 新。`ToolWrapSkill`。 |
| `src/jshi/tool/plan.py` | `ToolPlanner` 默认改为 skill；保留 `RulePlanner` 供测。 |
| `src/jshi/tool/service.py` | 策划走 205；launch 后按本通知包装队列；`list_visible` 读 `visible`+`summary`。 |
| `src/jshi/tool/hang.py` | `visible` / `summary`；阶段性事实通知；去掉叫醒语义。 |
| `src/jshi/tool/port.py` | Runner：执行中把反馈逐条写入 210，再通知包装。 |
| `src/jshi/tool/pi_engine.py` | 边读边交出事件（生成器或回调）；不改 Pi 内核。 |
| `src/jshi/tool/templates/` | 模板说明目录。 |
| `skills.json` | `tool_plan` / `tool_wrap`。 |
| `process.py` / 03 | **不改对外契约**（仍三口 + `tool_input`）。可选：注入两个 skill 的模型工厂。 |
| `doc/design/工具使用.md` 等 | **编码前**按本方案回写（确认方案后的单独一步）。 |

---

## 6. 数据结构（相对现状）

**HangRecord 增/改**

```text
visible: bool          # 原 notify_caller
summary: string        # 当前给 03 的那句；空则不算可见
wrap_meta: { model_tag, updated_at }   # 可放 meta
feedback: 仍只增
```

**阶段性事实**（给包装的切片，不进 03）

```text
kind: progress | result
text: 已剥思维链的短事实
at
```

**交接**不变。策划失败仍用 `status=failed` + `plan_error`。

---

## 7. 行为变更

- 主流程仍：写场 → `intake`（不等）→ 07。
- 03 仍只见 `summary`。包装 skill 没交出可见时，这一路不出现或没有该 `task_id`。
- 默认主流程引擎仍可以是 Stub；接 Pi 仍是配置/工厂的事，本方案不把 Pi 改成默认。
- 05 提示词不改任务：仍是「看见 `tool_input`，匠石决定说不说」。

---

## 8. 测试计划

在现有 `tests/test_tool_hang.py` 上加，不把 live Pi 当单测。

1. `intake` 后 RulePlanner/假 skill 失败 → 无 hang，`list_visible` 有失败句。
2. Stub 跑完 → 包装（假 skill 固定 `visible=true`）→ `tool_input` 等于 `summary`，不是 Pi/Stub 长 `result` 原文。
3. `visible=false` → `list_visible` 不含该 task。
4. 同一 `task_id` 两次包装 → 03 仍一条，内容为后一次 `summary`。
5. 两个 hang、两个对象 → 各自 `list_visible` 不串。
6. 第二次 `intake` 在第一次还在跑时能返回（可重入）。
7. `cancel` 一本不影响另一本。
8. 包装抛错 → 跟随仍写入 result；不进可见或走降级短句。
9. Pi 适配单测用录制事件：先 `update` 再 `end`，断言 hang 在 `end` 之前已有 progress 行（边读边入账）。不启动真 Pi。
10. `ToolPlanSkill` 解析失败 → 无 hang。选目录外模板名 → 当失败。

回归：`test_assembly.py`、`test_model_prompt.py`、`test_skill.py`（认知）不得被 200 skill 带坏。

---

## 9. 建议编码顺序（确认方案并回写设计之后）

1. **210 账本 + 206 边写入账 + 一本一通知**（包装仍规则占位：有 result 则 `visible`，`summary` 仍可先用短摘要）。
2. **`ToolWrapSkill`** 换掉规则摘要。
3. **模板说明 + `ToolPlanSkill`** 换掉 `RulePlanner`。

理由：没有边写入账，包装永远只能看整包结束；没有包装，03 仍吃引擎残片；策划可以后上，echo 仍能跑通通道。

---

## 10. 风险与未决

- 包装过密：必须「有阶段性成果才调」，否则费用与 `tool_input` 都会吵。阈值本刀用「有文本的 update / 任意终态」；过密再收。
- 一条 need 拆多条请求：本刀 skill 先出 1 条；多条是后续。
- 总并发上限数值：占位，用户拍板前用小整数（如同时包装 2）。
- 主流程是否默认 Pi：不在本方案改；仍 Stub 默认。
- 设计文档仍写 210「判断可见」：以本方案为准回写后才能编码。

---

## 11. 记录、评价与参数

- 记录：交接、定义（含 `model_tag`）、210 反馈流、每次包装的 `visible`/`summary`/`model_tag`。不进 16/09。
- 评价：可见过密过疏；包装失败率；空头支票（05 口头有、intake 无）仍后补。
- 参数：`list_visible` 上限 8；包装并发上限；`NEED_MIN_CHARS` 在 skill 接管后可废或仅作预检。

---

## 12. 回退

- 去掉两个 skill 注入，策划退回 `RulePlanner`，可见退回「有 RESULT 则可见」。
- 210 文件多出来的 `summary` 字段可忽略。
- 不回退 I-017 三口与 `tool_input`。

---

## 13. 牵连

- 改的：200 内部、skill 框架下两子类、`skills.json`、测试。
- 回写（确认后）：`doc/design/工具使用.md` §5–13、概念词汇「记挂」、00 地图 200 一句。03/05 对外契约不动。
- 不改：Pi 内核、10、08、16、09、叫醒。

---

## 14. 本刀之后仍不做

已编码（相对 §9 三刀之后）：配置切换引擎（默认 Stub）；205 读 `list_commands`；`command` 与模板名分开；墙钟超时；引擎不报价；`summary` 统一截断；`cancel` 停会话；jsonl 追加快照；`model_tag` 落账；进度包装保持 `open`。

205 找引擎命令：小目录整表；大目录 Hermes 式分层披露 + BM25 search/describe，见 `方案(coding)/205-工具检索.md`。

仍不做 / 后补：

- 可见句 TTL / 开口后再藏（G11）。03 契约不动。
- 300 自省完整入口。
- 主流程默认改 Pi。
- 100 / 15 / 11 权限。
- 改 Pi 内核。
- 一条 need 拆多条请求。

---

## 15. 已编码：反馈进片场（2026-09-11 调试实测后）

> 状态：**已按用户口径编码并回写设计**（`doc/design/工具使用.md` §4.8 / §12 / §13 / §14.3 / §15 / §17、`doc/design/03-状态组装.md` tool_input 行、`doc/概念词汇.md`「记挂」「工具块」）。
> 对外契约（`intake` / `list_visible` / `cancel`）不变；交付、回写、撤销交付都是 200 内部口。

**已编码**

1. **工具块**：200 修饰成 `（我知道）<一句人话>`；单条 240 字、同场最多 3 条（`service.SCENE_BLOCK_CHARS` / `SCENE_BLOCK_CAP` / `SCENE_BLOCK_PREFIX`）。写场时由程序 `append_text` 追加进该对象的片场；**不是「我说」**。
2. **对象专属**：只交付该对象可见的条目；`deliver_to_scene(subject, object)` 不跨对象。
3. **单独预算**：片场字数行分两档报（片场 / 工具块），工具块不占那 2000 字（`packs.format_zone_budget_note(tool_chars=, tool_cap=)`）。
4. **交付即离开 `tool_input`**：`list_visible` 跳过 `delivered_at` 非空的账本；交付只针对本轮真正装上的那几条（`_turn_tool_ids`，组装时记录）。
5. **撤销交付**：片场这轮既无输入也无回应时不追加，清掉交付标记，下一轮仍走 `tool_input`（结果不丢）。
6. **回应回写**：该轮可见的每一本追加 `responses`（`{mode, reply, action, text}`，只增）；没开口也留轮次。回写在写场之后、`intake` 之前。
7. **过期语义**：该条对当前场面过期＝随片场压缩掉落；不需要 TTL，记挂侧全量保留（§19 已记）。

**代码落点**：`tool/hang.py`（`delivered_at` / `delivered_block` / `responses`、`set_delivered` / `clear_delivered` / `set_response`）、`tool/service.py`（`format_scene_block` / `pending_for_scene` / `deliver_to_scene` / `write_back_response` / `revoke_delivery`、`list_visible` 跳过已交付）、`subject/process.py`（`_turn_tool_ids`、`_tool_scene_blocks`、`_revoke_tool_delivery`、`_tool_write_back`、`_tool_block_chars` / `_tool_block_cap`）、`style/packs.py`（字数分档）、`tests/test_tool_hang.py`（7 条新测，含不依赖 `tmp_path` 的）。

**仍待拍 / 仍不做**

- **P2**：包装 skill 直接产出片场书写形态（现由 200 规则加前缀；skill 的 instruction 待用户亲自调）。
- **P5**：策划失败摘要是否也进片场（现在仍走 `tool_input`，是那条无主语裸句）。
- **P6**：05 未开口时记 `mode` 已在代码里做了；`turn` 字段暂用 `activity` 语义待定。
- **P7**：回写触发方现在是主流程（`process`）在写场后调 200；自省入口接入时要一起看。
- 匠石如何判断「工具块已过期、该重查」仍只靠模型读片场，无程序信号。
- 木头的活跃区版本是否也接工具块（现只人格片场交付；木头靠 `tool_input` 且不撤销）。

> **用户口径（原文照记，防走样）**：来源＝对话实测（天气、机票两轮）。工具反馈不再只当「尚未出口的一段」，而是经 200 修饰成匠石当前知道的事实，直接进片场。

**已认同的口径**

1. 200 修饰后的反馈**进片场**，形态是「匠石当前知道的事实」，例如「我已经查了明天杭州的天气，是多云转晴……」。写场时匠石就在片场里，所以这类块进片场是合适的。
2. **对象专属**：不是当前对象产生的工具反馈，不得进该对象的片场。
3. 工具块**单独占一块字数预算**，不挤对白与情景。
4. **过期指「该条对当前场面过期」，不是该工具反馈过期。** 中间的反馈对 03 过期时，记挂照旧全量保留。
5. 一次工具使用的**反馈 + 05 的回应**都要回写进该工具的记挂（回应可以完全不提工具）。
6. **硬约束**：工具块**不得写成「我说：……」**。若写成「我说」，下一轮匠石会以为自己早已告诉对方——这正是 `doc/design/工具使用.md` §13「写场不带 `tool_input`，以免写进片场当已经出口」原本要防的错误。该条防线**只能改写、不能删**。

**当时的开口（现已按上面 1–7 收口）**

- `doc/design/工具使用.md:99`「不进片场」、`:246`「写场不带 `tool_input`」——实测策划失败句曾以**裸句**形态进 prompt，位置紧贴回忆之后（`process.py` 里 `tool_input` 追加在回忆段后、**无标题**）。
- `doc/design/03-状态组装.md:149` 原写「过期/说完再藏由 200 内部日后收口」——已按 §13 收口为「已交付不再走 `tool_input`，过期随片场压缩」。
- 现象后果：句子无主语、无来源标记，模型读到会分不清是谁说的（实测中表现为「这一问有点怪」）。**P5 仍开着**：策划失败摘要还是那条裸句。

**这条链给的三处结构收益（下次讨论时直接引用）**

- **片场就是唯一的「待交付队列」**：交付时点＝写进片场；过期＝随片场压缩自然掉落。不需要 TTL，也不需要 200 再维护一套「已交付/已过期」状态表。
- `tool_input` 的定位从「主交付」降为「**尚未进片场的那一条**」（即时性那一路）；03 的对外形状仍不变。
- 记挂成为**两段**：前半引擎跟随，后半 05 的回应。一本记挂＝一次工具使用的完整过程。

**回写设计的牵连（本轮已一并处理）**

- `doc/design/工具使用.md`：§4.8、§12、§13、§14.3、§15、§17、§19 已改。
- `doc/design/03-状态组装.md`：tool_input 行已改为「只出尚未交付的那些」。
- `doc/概念词汇.md`：已加「工具块」并改「记挂」（两段、交付、过期）。
- 写场侧（人格 `ZONE_WRITE_RULES` / `EDIT_RULES`，`src/jshi/style/packs.py`）：字数行已分档并写明「（我知道）不是我说」。
- 记挂导出侧：`src/jshi/tool/view.py`（`/tool`、`jshi tool-log`）**尚未**读出 `delivered_*` 与 `responses`；`/tool` 里「03 将装」是事后重算当前 `list_visible`，看历史那轮会骗人，待改名或改记录。
